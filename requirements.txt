# TTS Demo - Python 依赖
# Python 3.10+ 推荐

# Web 框架
Flask>=3.0.0

# 离线 TTS 引擎
sherpa-onnx>=1.10.0

# ChatTTS（可选，需要 GPU 或较强的 CPU）
# chattts>=0.1.0

# 音频处理
numpy>=1.24.0
scipy>=1.10.0

# 在线 TTS 对比（可选）
edge-tts>=6.1.0

# 系统级 TTS（可选）
pyttsx3>=2.90